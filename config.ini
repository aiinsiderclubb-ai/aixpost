[Credentials]
# Credentials are loaded from the web dashboard / database.
# Optional env overrides: FB_USERNAME, FB_PASSWORD
username =
password =

[Settings]
max_groups_per_session = 200
min_delay_seconds = 10
max_delay_seconds = 60
batch_size = 10
headless_mode = false

[Templates]
use_templates = true
template_mode = random
log_template_usage = true

[Telegram]
# Optional env overrides: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
telegram_bot_token =
telegram_chat_id =
notifications_enabled = false
errors_only = false
